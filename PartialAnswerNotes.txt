Scope:
- basic hello world
- explain how parts of spring boot work
- 

>>>>
-when using version, safe to do is to use one version below (stable version)

by default spring runs the web app on port 8080.

front end >> resource/template folder

resources (images) >> resource/static folder

SERVICE >> holds the business rule codes
CONTROLLER >> 

>>>>>>>>>>>>>>>>>>.

sudo gedit ~/.bashrc
//then 
export FIRST_NAME="Irvin Gil" //edit .bashrc via gedit

//then 
source ~/.bashrc

//then
echo $FIRST_NAME

//then run the application
sh ./gradlew bootRun

